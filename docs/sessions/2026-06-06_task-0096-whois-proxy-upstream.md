# 2026-06-06 — TASK-0096: upstream хостовых патчей 0095 в whois-proxy + редеплой бота

**Дата:** 2026-06-06 · **Таск:** TASK-0096 · **Исполнитель:** Claude
(хосты + whois-proxy) · **Отчёт собран архитектором** из вывода
исполнителя (сессия исполнителя завершилась без push отчёта) +
независимой проверки git.

## Выполнено (по выводу исполнителя)

- **whois-proxy**: хостовые правки 0095 версионированы:
  - `34ec1b8` — upstream TASK-0095 host patches (ADR 046): server.py
    (594+/272-), `systemd/whoisd-edge.service`, `systemd/whoisd-main.service`;
    дефолты `RU_UPSTREAM_TTL_NEG=60`, `CACHE_TTL_NEG=60`, RDAP-404 → no-data,
    `BIND_ADDR_WHOIS`.
  - `5d19325` — fix: 'no match' возвращён в `NO_DATA_RE` (выпал при
    переписывании).
- **Прод-хост**: whoisd active, verified (хосты перекатаны из git).
- **VDS**: whoisd active, verified.
- **Бот редеплоен**: commit `1957328` (включает TASK-0094 — free всегда
  live — и email-фикс `fetched_at` d7c7172).
- **Бекап БД до деплоя**: `/home/whoiswatcher/backups/manual-pre-0096/`.

## Независимая проверка архитектора

- whois-proxy `HEAD == 5d19325` — подтверждено (`git ls-remote` + clone,
  diff `2ae4442..HEAD` просмотрен: TTL_NEG=60 дефолты на месте).
- Заявленные active/verified на хостах и `/version` бота из песочницы
  архитектора недостижимы — принято со слов исполнителя; владельцу:
  контрольный `/version` в боте → `1957328`.

## Примечания архитектора

1. В `34ec1b8` дефолт `BIND_ADDR_HTTP` сменился `127.0.0.1` → `0.0.0.0`
   (комментарий: «exposed via ufw deny-by-default»). Отражает реальную
   прод-конфигурацию (docker-контейнерам нужен доступ к whoisd через
   host gateway), но это security-relevant изменение: проверить при
   следующем аудите, что ufw на обоих хостах действительно закрывает
   8043 снаружи.
2. Процессное: исполнитель второй раз подряд завершает сессию без push
   отчёта/статуса в whois-watcher (источник правды — GitHub). Отчёт и
   `done` проставлены архитектором.

## Эффект (ADR 046 полностью в силе)

«Свободен» нигде в пайплайне не живёт в кэше дольше 60 секунд:
бот (0094) — free всегда live; whoisd прод + VDS (0095, теперь из git
0096) — негативы ttl ≤60 с дедупом. Инцидент класса discozavr.ru
воспроизвести нельзя: после регистрации домена бот увидит «занят»
в течение ~минуты.

## Ссылки

- TASK-0094/0095 (предыдущие шаги ADR 046), TASK-0093/`666af2a` (урок
  «хостовые патчи — только версионированные»)
- whois-proxy: `34ec1b8`, `5d19325`

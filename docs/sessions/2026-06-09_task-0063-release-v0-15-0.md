# Сессия 2026-06-09 — TASK-0063: релиз v0.15.0 (админ/ops-слой)

**Дата:** 2026-06-09 · **Таск:** TASK-0063 · **Исполнитель:** архитектор (напрямую на main) · **Тег:** `v0.15.0`

## Что сделано

- `pyproject.toml`: bump `version` 0.14.0 → 0.15.0.
- `CHANGELOG.md`: секция `## [0.15.0] — 2026-06-09` (бекапы Postgres, ежечасный
  ops-отчёт, дневные графики 21:00 МСК, аудит-лог; deploy-нюансы).
- Аннотированный тег `v0.15.0` на main, запушен.
- `STATE.md` обновлён; TASK-0063 → done.

## Контекст

Админ/ops-слой (ADR 042). Аудит TASK-0062 — вердикт GO. Закрывает пробел,
вызвавший недавнюю потерю данных (автоматических бекапов не было).

## Деплой (обязательно)

- Пересобрать образ — добавлен `postgresql-client-16`.
- Создать docker-том `ww_backups`, смонтирован на worker/scheduler.
- Выставить env: `BACKUP_DIR`, `BACKUP_KEEP=36`, `BACKUP_MIN_BYTES`,
  `AUDIT_RETENTION_DAYS=90`.
- Подтвердить зелёный CI перед тегом/деплоем (локальный pytest в песочнице
  архитектора недоступен — нет Python 3.11).

## Осталось

- Деплой v0.15.0 (+ v0.14.0, если ещё не катился).
- Опц. TASK-0065 (офсайт/шифрование бекапов).
- Следующий этап — WebApp (ADR 043, TASK-0064).

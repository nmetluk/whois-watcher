# SESSION-0080 — Релиз v0.15.2 (TASK-0080)

**Дата:** 2026-06-10 · **Таск:** TASK-0080 · **Роль:** архитектор (релиз напрямую)

## Сделано

- Смержен TASK-0079 (DNS-сбой ≠ «нет записей» в email-слое) в `main` через
  merge-commit (PR-flow, без прямого push исполнителем).
- Bump `pyproject.toml` 0.15.1 → 0.15.2.
- `CHANGELOG.md` — секция `[0.15.2]` (Fixed: dns_unreachable вместо ложного
  «MX нет»; Added: `DNS_NAMESERVERS`, `build_resolver`/`classify_dns_exc`,
  диаг-логи).
- `docs/deployment.md` — Troubleshooting-раздел «MX / email-блок пуст»:
  диагностика резолва из контейнера воркера + настройка `DNS_NAMESERVERS`
  (+ ufw 53). `.env.example` уже содержит пример (из 0079).
- Аннотированный тег `v0.15.2` на `main`.

## Проверки

- TASK-0079 локально 14/15 (1 фейл — `datetime.UTC` на py3.10 в песочнице,
  в CI/3.11 зелёный); ключевой регресс `test_mx_lifetime_timeout_is_dns_unreachable`
  проходит. `handoff.py validate` зелёный.

## Открыто (ops, не код)

- Подтвердить прод-диагностикой, резолвит ли воркер DNS. Если нет —
  `DNS_NAMESERVERS=1.1.1.1,8.8.8.8` + ufw-allow 53 из `172.28.0.0/16`.

## Следующий шаг

- Деплой v0.15.2; затем webapp-цепочка 0073 → 0071 → 0072.

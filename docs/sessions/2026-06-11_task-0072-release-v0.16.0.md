# SESSION-0072 — Релиз v0.16.0 (Telegram WebApp)

**Дата:** 2026-06-11 · **Таск:** TASK-0072 · **Роль:** архитектор (релиз напрямую)

## Сделано

- Подтверждены закрытыми все депенденси: 0071 (аудит, FIX-THEN-GO) + блокеры
  0081/0082/0083 + ниты 0084. Все смержены в `main`, ревью архитектора.
- Bump `pyproject.toml` 0.15.2 → 0.16.0.
- `CHANGELOG.md` — секция `[0.16.0]` (Added: WebApp/mini-app, backend
  `/api/webapp`, группы/теги, CSV-импорт; Security: initData/ownership/CSP;
  Internal: ADR 043, цепочка тасок).
- Аннотированный тег `v0.16.0` на `main`.

## Содержимое релиза

WebApp-слой (ADR 043): дашборд/список/карточка/календарь/алерты/«Ещё»,
backend read+write+auth (initData HMAC + TTL), группы/теги доменов, CSV-импорт.
Аудит безопасности 0071 (F1–F10 закрыты).

## Проверки

- 0073/0079/0081/0082/0083/0084 — ревью + локальные прогоны тестов (webapp_auth,
  groups unit, dns-classify) зелёные; api.py-оверлапы слиты чисто; uv.lock
  ресинкнут с pyproject (matplotlib).
- `handoff.py validate` зелёный.

## Следующий шаг

- Деплой v0.16.0 (образ + статика webapp через nginx + `WEBAPP_ORIGIN`/
  `WEBAPP_INITDATA_TTL`; mini-app зарегистрировать в BotFather). Перед деплоем —
  ручной бекап БД (миграция `20260610_domain_group`).
- Дальше — v1.0 (web-дашборд вне Telegram, публичный API, орг-аккаунты,
  Prometheus).

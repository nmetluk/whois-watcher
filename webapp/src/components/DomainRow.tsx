import React from 'react'; import { statusOf, puckText, daysText } from '../lib/domain'; export const DomainRow: React.FC<any> = ({ d, onOpen }) => { const s = statusOf(d); return <div className="tg-drow" onClick={() => onOpen(d)}><div className="tg-puck" style={{ background: s.dot + '22', color: s.dot }}>{puckText(d)}</div><div className="tg-drow-info"><div className="tg-drow-name">{d.unicode}</div><div className="tg-drow-sub">{d.registrar || '—'} · {daysText(d)}</div></div><div style={{ textAlign: 'right' }}><div>{d.health}</div></div></div>; };

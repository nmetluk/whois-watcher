import React from 'react'; export const Ring: React.FC<any> = ({ value, size = 56, stroke = 6, showLabel = true, label }) => { const r = (size - stroke) / 2; const c = 2 * Math.PI * r; const off = c * (1 - Math.max(0, Math.min(100, value)) / 100); const col = value >= 75 ? 'var(--pv-green)' : value >= 45 ? 'var(--pv-gold)' : 'var(--pv-red)'; return <div className="tg-ring" style={{ width: size, height: size }}><svg width={size} height={size}><circle cx={size/2} cy={size/2} r={r} fill="none" stroke="var(--pv-muted)" strokeWidth={stroke} /><circle cx={size/2} cy={size/2} r={r} fill="none" stroke={col} strokeWidth={stroke} strokeLinecap="round" strokeDasharray={c} strokeDashoffset={off} style={{ transition: 'stroke-dashoffset 0.6s ease' }} /></svg>{showLabel && <div className="tg-ring-val" style={{ fontSize: size * 0.3 }}>{Math.round(value)}{label && <span style={{ fontSize: size * 0.13, color: 'var(--pv-fg-muted)' }}>{label}</span>}</div>}</div>; };
